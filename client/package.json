{
  "name": "institution-restaurant-client",
  "version": "1.0.0",
  "private": true,
  "description": "Client workspace for Institutional Restaurant Platform",
  "scripts": {
    "dev": "npm --prefix user-portal run dev",
    "build": "npm --prefix user-portal run build",
    "preview": "npm --prefix user-portal run preview"
  }
}
